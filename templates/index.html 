{% extends "base.html" %}
{% block content %}
  {% if articles %}
    {% for article in articles_page.object_list  %}

      <article class="post">
        <header class="post-header">
          <h1><a href="{{ SITEURL }}/{{ article.url }}">{{ article.title }}</a></h1>
          <div class="post-time">{{ article.date }}, Make a plugin. Words</div>
        </header>

        <div class="post-after">
        {% if article.tags %}
          <div class="tags">
          {% for tag in article.tags  %}
                <a href="{{ SITEURL }}/tags/{{ tag.url }}">{{ tag }}</a>
          {% endfor %}
          </div>
        {% endif %}
        </div>
      </article>

    {% endfor %}
{% include !simple/pagination.html %}
{% endblock %}
